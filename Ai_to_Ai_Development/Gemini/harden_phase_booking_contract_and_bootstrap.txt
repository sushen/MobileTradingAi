You are a senior Android and Firebase engineer working inside an existing Kotlin Android app repository. Execute one task only: harden the remaining phase-booking Firestore contract gaps so the booking flow is both reliable and safer within the current client-side architecture.

Treat context.md as ground truth. This project must remain client-side only: Kotlin Android app, Firebase Auth, and Firestore rules only. Do not add any backend server, Cloud Functions, or external services. Admin is identified only by the fixed email sushen.biswas.aga@gmail.com. Relevant existing components are FirestoreAppStore, PhaseRepository, PhaseCatalog, and firestore.rules. Current unresolved issues from context are: any signed-in user can still increment or decrement phases/{phaseId}.bookedSeats by 1; phase booking can fail if phase documents were never pre-created because ensurePhasesSeeded() is brittle and requestSeat() uses transaction.update(); and booking state transitions still need tighter alignment between Android client writes and Firestore rules.

Implement the smallest production-quality change set that fixes this specific area only. Make phase document availability deterministic from PhaseCatalog so missing phase docs no longer break booking. Tighten firestore.rules so students cannot arbitrarily mutate seat counters, while still allowing the legitimate client-side flow for booking creation, owner expiry, and admin approve, reject, and cancel actions. Keep classroom unlock behavior compatible with users/{uid}.unlockedPhases. Use Firestore transactions or batched writes where necessary, and make state transitions idempotent so seats cannot double-increment or go negative. Preserve the fixed 15-minute pending window and existing booking statuses.

Stay focused on this task. Do not redesign the product. Do not expand into affiliate features, advanced features, analytics, or UI polish unless a minimal code adjustment is strictly required to complete this fix correctly. Before changing rules, verify the actual Kotlin write paths that touch phases, bookings, and unlocked phase access so the final contract matches real app behavior.

In your response, return only four things: a concise implementation plan, the exact files changed, patch-ready diffs or full final file contents for each changed file, and a short verification checklist covering missing-phase bootstrap, student booking creation, owner expiry, admin approval, admin rejection or cancellation, seat-count integrity, and classroom access after approval.
