You are a senior Android and Firebase engineer working inside this repository. Execute one task only: harden and align the Firestore security contract with the existing client-side app flow so the app remains functional and safer without any backend or Cloud Functions.

This project is client-side only. All logic must stay inside the Kotlin Android app and Firebase Auth/Firestore rules. Admin is identified only by the fixed email sushen.biswas.aga@gmail.com. The app lets students sign in with Google, browse six phases, request a seat, wait for admin approval, enter classrooms, complete lessons, update progress, and participate in referral and affiliate tracking. The current context says the biggest risk is the Firestore contract: signed-in users can tamper with phases.bookedSeats and affiliateStats, expired pending bookings cannot be cleanly written to expired by the owner, and rules are brittle around phase/booking flows.

Implement the smallest production-quality change set that fixes this contract while preserving the current architecture and client behavior. Do not introduce servers, Cloud Functions, or external services. Keep the email-based admin model. Focus first on firestore.rules. Allow only the exact owner writes needed for student profile updates, lesson completion, booking creation, and booking expiry or cancellation. Preserve admin-only approval, rejection, cancellation, and privileged user access changes. Prevent arbitrary user writes to seat counts and affiliate stats while keeping the existing referral conversion flow usable in a client-only design. If a minimal client change is absolutely necessary to match the new rules, make it, but keep the task centered on the Firestore contract.

Use the current context.md as ground truth. Do not redesign the product. Do not add unrelated improvements. Check the Kotlin code paths that touch users, phases, bookings, referralEvents, affiliateStats, and lesson progress before changing rules.

Return only the final implementation result: the exact file changes, a brief explanation of the new Firestore contract, any minimal client code changes required, and the specific student/admin flows you verified logically against the updated rules.
