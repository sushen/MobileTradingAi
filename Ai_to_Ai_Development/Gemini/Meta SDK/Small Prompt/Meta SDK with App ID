You are an expert Android developer. Integrate the Meta (Facebook) Android SDK into my project using the following App ID.

### App Details

* Facebook App ID: 1603183510975171

---

### Tasks

1. Add Meta SDK dependency in `app/build.gradle`:

```gradle
dependencies {
    implementation 'com.facebook.android:facebook-android-sdk:latest.release'
}
```

Ensure `mavenCentral()` is enabled.

---

2. Add App ID in `res/values/strings.xml`:

```xml
<string name="facebook_app_id">1603183510975171</string>
```

---

3. Update `AndroidManifest.xml`:

Add permissions:

```xml
<uses-permission android:name="android.permission.INTERNET"/>
```

Inside `<application>`:

```xml
<meta-data android:name="com.facebook.sdk.ApplicationId"
    android:value="@string/facebook_app_id"/>

<meta-data android:name="com.facebook.sdk.AutoInitEnabled"
    android:value="true"/>

<meta-data android:name="com.facebook.sdk.AutoLogAppEventsEnabled"
    android:value="true"/>
```

---

4. Initialize SDK automatically (no manual Application class unless needed)

---

### Goal

* SDK should initialize successfully
* No crashes
* Ready for basic event tracking

---

### Constraints

* Do NOT use deprecated APIs
* Keep implementation minimal and clean
* Do NOT require SHA key for this setup step
* Focus only on SDK initialization

---

### Output

* Updated Gradle file
* Updated Manifest
* strings.xml updated
* Project builds successfully
