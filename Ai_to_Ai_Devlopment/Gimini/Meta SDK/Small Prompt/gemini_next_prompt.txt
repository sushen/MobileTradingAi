You are an Android developer working on an existing Android project.

## Context

The project is a native Kotlin Android app for AI trading education with progressive learning phases, manual seat booking, and classroom progress. `AI-context.md` is the primary source of truth: it says the repo is incomplete and currently not buildable, but the phases/classroom business flow is the most developed area. `README.md` supports the same intended six-phase, approval-based learning flow, but broader README claims such as Cloud Functions and complete auth should not be treated as implemented.

The existing Kotlin code already includes `PhasesFragment`, `PhaseViewModel`, `PhaseAdapter`, `ClassroomFragment`, `ClassroomViewModel`, `ClassroomViewModelFactory`, `LessonAdapter`, and `PhaseRepository`. The flow is blocked because required XML resources are missing: `fragment_phases.xml`, `dialog_booking_request.xml`, `item_phase.xml`, `fragment_classroom.xml`, `item_lesson.xml`, plus lock drawables `ic_lock_open` and `ic_lock_closed`.

## Task

Implement this one feature:
Restore the missing Fragment/XML resources for the phases, booking, and classroom flow

## Goal

Add the missing layout and drawable resources required by the existing phases and classroom Kotlin code so ViewBinding can generate the expected binding classes and the screens can render.

Keep the work limited to the existing phases -> booking request -> classroom UI path. Do not attempt to complete authentication, Firebase configuration, admin approval, Cloud Functions, `AppStore`, `PhaseCatalog`, or other app-wide missing systems in this task.

## Requirements

- Follow the existing Android architecture already used in the project
- Reuse existing repositories, models, fragments, adapters, and viewmodels where possible
- Keep changes minimal and focused
- Do not refactor unrelated code
- Stay consistent with current Firebase / Firestore usage
- Use Fragment/XML-based UI unless the context clearly proves Compose is already used
- Respect the current package structure and naming conventions

## Implementation Steps

1. Identify the exact files that need to change
2. Explain what must be added or modified in each file
3. Implement UI changes if needed
4. Implement ViewModel logic
5. Implement repository / Firestore logic if needed
6. Handle loading, success, and error states
7. Handle validation and edge cases
8. Make sure the code is consistent with the current project structure

For this task, first inspect the existing Kotlin binding references in:

- `app/src/main/java/com/shaplachottor/app/fragments/PhasesFragment.kt`
- `app/src/main/java/com/shaplachottor/app/adapters/PhaseAdapter.kt`
- `app/src/main/java/com/shaplachottor/app/fragments/ClassroomFragment.kt`
- `app/src/main/java/com/shaplachottor/app/adapters/LessonAdapter.kt`

Use those references to determine the exact required view IDs and view types. Then create or restore only these resource files unless the code proves another directly related resource is required:

- `app/src/main/res/layout/fragment_phases.xml`
- `app/src/main/res/layout/dialog_booking_request.xml`
- `app/src/main/res/layout/item_phase.xml`
- `app/src/main/res/layout/fragment_classroom.xml`
- `app/src/main/res/layout/item_lesson.xml`
- `app/src/main/res/drawable/ic_lock_open.xml`
- `app/src/main/res/drawable/ic_lock_closed.xml`

Use Material-compatible XML views and existing project colors, strings, dimensions, and themes where available. If a referenced string is missing and the text is specific to this UI, add the smallest necessary string resource. Avoid changing Kotlin code unless a minimal binding mismatch fix is strictly required.

## Edge Cases

- XML view IDs must exactly match existing Kotlin binding references.
- Booking dialog phone and WhatsApp fields must be editable inputs compatible with current validation.
- Phase cards must support locked, unlocked, available, full, pending booking, approved booking, and expired booking states already handled by `PhaseAdapter`.
- Classroom layout must support loading, access denied, empty lesson list, progress display, and lesson completion states expected by `ClassroomFragment` and `LessonAdapter`.
- Long phase titles, descriptions, lesson titles, and lesson descriptions must wrap cleanly on small screens.
- Empty lists and missing optional text should not crash rendering.
- The repo has a known `.gitignore` issue that ignores `*.xml`; still create the correct resource files in the Android resource directories.

## Output Format

- Provide full code, not partial snippets
- Clearly label each file path
- Keep explanations brief and technical
- Do not include unrelated improvements

## Constraints

- Do not break existing features
- Do not invent missing systems unless required for this task
- Do not perform broad refactors
- Do not change unrelated architecture
- Keep the task small enough to complete in one focused iteration

## Why this task

This is the correct next step because the phases and classroom flow is the most complete core workflow in the current code, but it is blocked by missing XML and drawable resources. Restoring these files is a focused, realistic step that lets the existing Fragment/ViewModel/Adapter code bind to real UI and unlocks the next focused task: completing the missing data/session abstractions needed for a runnable happy path.
